# Tools

- `run-agent-bridge.ps1` builds a temporary harness mod, runs a deterministic Factorio scenario through `agent_bridge`, and writes run artifacts under `.factorio-test/user-data/script-output/agent-bridge-runs/<run-id>/`.
